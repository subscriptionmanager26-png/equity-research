# Python packages baked into the Cursor Cloud Agent environment.
# Installed by scripts/install-agent-env.sh during environment install / builds.
# Telegram-triggered agents should import these directly — do not pip install them per run.
fpdf2>=2.8.2,<3
Pillow>=10.0.0
fonttools>=4.50.0
reportlab>=4.2.0
